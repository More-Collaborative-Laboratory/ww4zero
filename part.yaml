part:
  title: Part
  type: object
  description: Identifies a constituent parts of a project 122
  properties:
    id:
      description: Identifies the part's name of a project
      type: string
      format: uri
      example:  urn:ngsi-ld:id:[MC_MUEBLETV_A2_RIPA_TRAS]
      x-ngsi:
        type: Property

    name: 
      description: Identifies the part's name that belongs to a specific project - REF PEÇA (A)
      type: string
      example:  MC_MUEBLETV_A2_RIPA_TRAS
      x-ngsi:
        uri: "https://schema.org/name"
        uri-prefix: "https://schema.org/"
        model: https://schema.org/name
        type: Property



            
required:
- id
